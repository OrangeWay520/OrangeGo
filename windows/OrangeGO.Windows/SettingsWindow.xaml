<Window x:Class="OrangeGO.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OrangeGO 设置" Width="430" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 基本信息 -->
        <StackPanel>
            <TextBlock Text="基本信息" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock Text="设备别名" FontSize="13" Margin="0,12,0,0"/>
            <TextBox x:Name="NameBox" FontSize="13" Margin="0,6,0,0" Padding="10,7"
                     Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
        </StackPanel>

        <!-- 接收 -->
        <StackPanel Grid.Row="1" Margin="0,18,0,0">
            <TextBlock Text="接收设置" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock Text="收件目录" FontSize="13" Margin="0,12,0,0"/>
            <Grid Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="DirBox" FontSize="13" Padding="10,7" VerticalContentAlignment="Center"
                         Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                         BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                <Button Grid.Column="1" Content="浏览…" Style="{StaticResource GhostButtonStyle}" Padding="14,7"
                        Margin="8,0,0,0" Click="BrowseDir_Click"/>
            </Grid>
        </StackPanel>

        <!-- 外观 -->
        <StackPanel Grid.Row="2" Margin="0,18,0,0">
            <TextBlock Text="外观" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <StackPanel Margin="0,8,0,0">
                <RadioButton x:Name="ThemeFollow" GroupName="Theme" Content="跟随系统" FontSize="13" Margin="0,6,0,0"/>
                <RadioButton x:Name="ThemeLight" GroupName="Theme" Content="浅色" FontSize="13" Margin="0,8,0,0"/>
                <RadioButton x:Name="ThemeDark" GroupName="Theme" Content="深色" FontSize="13" Margin="0,8,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- 启动 -->
        <StackPanel Grid.Row="3" Margin="0,18,0,0">
            <TextBlock Text="启动" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <CheckBox x:Name="AutoStartBox" Content="开机时自动启动 OrangeGO（后台常驻）" FontSize="13" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- 底部操作 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="取消" Style="{StaticResource GhostButtonStyle}" Width="92" Click="Cancel_Click"/>
            <Button Content="保存" Style="{StaticResource PrimaryButtonStyle}" Width="92" Margin="10,0,0,0" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>